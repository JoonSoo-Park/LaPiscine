printable characters in C

A printable character is a character tha occupies a printing position on a display

For the standard ASCII character set (used by the "C" locale), printing characters are all
characters are all with an ASCII code greater than 0x1f (US), except 0x7f (DEL).
